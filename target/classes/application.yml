spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/my_finance
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    generate-ddl: true

